@startuml 报名缴费流程-活动图
' ============================================================
' A3 建模智能体 — 活动图：报名缴费流程
' 来源: 课程顾问+财务人员 需求记录
' ============================================================

title 报名缴费流程（含优惠与试听）

start

:家长咨询/到访;
:课程顾问登记线索;

if (是否有试听需求?) then (是)
  :安排试听课;
  :试听后跟进;
  if (家长满意?) then (否)
    :记录为潜在客户;
    :定期跟进提醒;
    stop
  else (是)
  endif
else (否)
endif

:选择课程与班型;
:计算费用（原价）;

if (是否有优惠?) then (是)
  :选择优惠策略\n(早鸟/团购/连报/老带新);
  :校验优惠叠加规则;
  if (叠加合法?) then (是)
    :应用优惠\n计算折后价;
  else (否)
    :触发优惠审批;
    if (校长审批通过?) then (是)
      :应用优惠;
    else (否)
      :使用原价;
    endif
  endif
else (否)
  :使用原价;
endif

:生成订单;
:家长选择缴费方式\n(微信/支付宝/银行/现金);

if (在线支付?) then (是)
  :调用支付网关;
  if (支付成功?) then (否)
    :标记待支付\n发送提醒;
    if (超时未付?) then (是)
      :订单自动取消;
      stop
    else (否)
      :家长重新支付;
    endif
  else (是)
  endif
else (现金/银行)
  :财务人工确认收款;
endif

:订单状态→已支付;
:发送报名成功通知\n(短信+小程序);
:分配班级/生成学员档案;

:财务记录收款\n生成财务凭证;
if (是否预收款?) then (是)
  :记录为递延收入\n待消课后逐笔确认;
else (否)
  :直接确认收入;
endif

stop

@enduml
